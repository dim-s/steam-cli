---
status: current
name: Active 3
---

## Команда nextfest: агрегатная статистика по участникам Steam Next Fest

---
id: SC-5
type: feature
status: todo
order: 100
notes:
  - id: n1
    text: "ИСТОЧНИК ПОДТВЕРЖДЁН ПО СКРИНУ СТРАНИЦЫ (16.06). tagids_featured=1440505 = ровно список «ВСЕ ИГРЫ» страницы феста: nMatchCount=4358 совпал со скрином (4357 показано + «Скрыто:1»), и фасет-счётчики жанров из rgSolrFacetCounts совпали ОДИН-В-ОДИН (Казуальная 597=2015, Экшен 19=1647, Приключение 21=1541, Стратегия 9=1308, Симулятор 599=1273). Источник верный. НЮАНС про Onimusha (поймал владелец): 2638890 присутствует в backend-пуле 4358, но владелец не видит её на странице — расхождение на УРОВНЕ ОТОБРАЖЕНИЯ (страница прячет 1 шт «Скрыто:1»; + режимы ГЛАВНАЯ=discovery/random/«Подобраны для вас» vs ВСЕ ИГРЫ=полный фильтруемый список — Onimusha не всплыла в discovery-выборке). На уровне данных она в списке. Если нужен «чистый» список — учесть, что в пул могут попадать отдельные паблишер/featured-демки. 1440505 в обычном search/results даёт 0 (внутренний sale-маркер, не community-тег). Чистого отдельного key-free реестра нет (куратор 39049601 = Steam Promotions, без app-списка) — но он и не нужен: ajaxgetdynamicsaleitems и есть то, что рендерит сама страница."
    createdAt: "2026-06-16T15:44:42.211Z"
---

ЗАЧЕМ: дать одной командой срез по текущему Next Fest (рецепт-аналитика феста: размер, топ по онлайну/отзывам, гистограмма тегов, распределение цен, даты релизов). Сейчас CLI не умеет перечислить участников события — browse фильтрует по тегам, specials/top-sellers по featuredcategories, страница /sale/nextfest это JS-рендер без appid в HTML.

ПРОВАЛИДИРОВАНО (16.06.2026, реально дёргал key-free обычным curl):
1) Список участников — GET store.steampowered.com/actions/ajaxgetdynamicsaleitems?tagids_featured=<TAGID>&max_results=10000&cc=<cc>&l=<lang> -> JSON {rgCapsules:[{id,type}], nMatchCount, bMoreResultsRemaining}. Июньский фест 2026: nMatchCount=4358, без пагинации. Ключ не нужен.
2) Авто-дискавери TAGID — GET /sale/nextfest, в HTML есть "featured_app_tagid":1440505 (СВОЙ у каждого феста, не хардкодить; regex featured_app_tagid"?\s*:\s*(\d+); плюс флаг --tagid override).
3) Обогащение — key-free батч IStoreBrowseService/GetItems/v1?origin=...&input_protobuf_encoded=<ids> (name/price/tags/release/обзоры пачкой; protobuf-кодирование списка id). Альтернатива — существующие overview/appdetails, но 4358 игр поштучно дорого.

ВАЖНЫЙ НЮАНС appid: в rgCapsules id — это appid ИГРЫ (coming_soon), а живой сигнал феста (онлайн players, отзывы по демо) висит на appid ДЕМКИ — её достаём из appdetails(filters=demos).demos[].appid. Проверено: Echoes of Aincrad игра 2244210 -> демо 4148250 (13054 онлайн в моменте), Onimusha 2638890 -> демо 3974650.

СТАТУС СОБЫТИЯ (из rtime32 на странице): фест ИДЁТ 15.06–22.06.2026 (демки играбельны, онлайн/отзывы копятся live). Заголовок страницы помечен "(предпоказ)" — это ложный сигнал, окно события активно.

КАВЕАТЫ ДИЗАЙНА: 4358 участников — НИКОГДА не обогащать всех по умолчанию; дефолт = список+счётчик+агрегаты из дешёвого батча, --limit/--sort (напр. by-players) для enrich топ-N. Эндпойнты first-party (никакого SteamSpy/SteamDB) — вписывается в правила проекта. Куратор 39049601 get_appids=true НЕ отдаёт appid (тупик). Нужны offline-фикстуры в стиле сьюта + bump __version__/pyproject.

## Команда ai <game>: AI-раскрытие игры со стор-страницы

---
id: SC-6
type: feature
status: todo
order: 200
notes:
  - id: n1
    text: "ГРАБЛИ С КЕШЕМ (поймал владелец): http_get(...) по умолчанию cache_ttl=0, а в steam_cli ttl=0 = НИ чтения, НИ записи кеша (_cache_get/_cache_put оба выходят на ttl==0). http_json передаёт DEFAULT_TTL, поэтому JSON-команды кешируются, а прямой http_get — нет. Команда ai ДОЛЖНА явно передавать положительный cache_ttl (DEFAULT_TTL ~6ч для свежести, или дольше для замороженного снимка) — иначе каждый вызов бьёт Steam заново и не сохраняет HTML. Проверено: с cache_ttl=math.inf 2-й вызов 0мс (кеш), тело пишется. Кеш-дир ~/Library/Caches/steam-cli."
    createdAt: "2026-06-17T05:52:20.601Z"
---

ЗАЧЕМ: AI Generated Content Disclosure — растущий per-game атрибут (Valve обязал с 2024). Переиспользуемый атом: любому анализу игры полезно «раскрыл ли AI и как». Встаёт в общий ряд per-game команд (info/reviews/players/tags). Решено владельцем делать, но НЕ сейчас (приоритет — разовый анализ феста скриптом).

ПРОВАЛИДИРОВАНО на пробе (analysis/nextfest-2026-06/nf_ai_crawl.py — оттуда лифтить парс):
- Источник: HTML стор-страницы /app/<id>/?l=english (GetItems AI-поля НЕ отдаёт — проверено). Прецедент HTML-скрейпа в CLI уже есть: tags/similar/browse.
- Якорь презенса: строка "how their game uses AI Generated Content like this:"; текст разработчика — в следующем <p><i>...</i></p> (вся декларация, в т.ч. многопунктовая, в одном <p>; <br> сохранять как разделитель пунктов).
- Нужна mature-кука (birthtime=0; lastagecheckage=...; wants_mature_content=1) — иначе взрослые игры отдают age-gate-заглушку ~30КБ. С кукой полная страница ~100-146КБ.
- Кеш уже бесплатный через http_get (HTML кешируется как для similar).

ФОРМА: steam-cli ai <game> --json -> {appid, status (ai_disclosed/no_disclosure/age_gated/unreachable), disclosure_text}. ТОЛЬКО по одной игре. Массовый краул по фесту остаётся analysis-скриптом (оркестрация поверх атома), НЕ командой.

ХРУПКОСТЬ (главный риск): якорь — английская фраза, ломается при реверстке/локали. ОБЯЗАТЕЛЬНО offline-фикстур-тест (HTML с декларацией / без / age-gated) в стиле сьюта + мягкая деградация (age-gate -> честный статус, не краш). Не over-generalize в «скрейп произвольных полей» — только этот атрибут. Бамп __version__/pyproject.

## Имена и описания ачивок + скрытые ачивки + глобальная статистика через ключ

---
id: SC-21
type: feature
status: todo
order: 400
links:
  - type: relates
    to: SC-20
  - type: relates
    to: SC-23
---

ЗАЧЕМ: README САМ документирует этот пробел («achievements returns internal API ids, not display names — human-readable names/descriptions require a Web API key (GetSchemaForGame)», строки 229-230) — не новая территория, а давно названная и теперь реализуемая недостача.

ЧТО ДАЁТ (проверено живым ключом владельца): GetSchemaForGame — полная схема ачивок ВКЛЮЧАЯ скрытые (hidden-флаг) + определения статов; GetGlobalStatsForGame — глобальная агрегатная статистика по игре.

ВАЖНО ПРО SCOPE (identity-safe часть хода): это СТАТИЧНЫЕ данные дизайна игры, не персональные данные другого игрока — работает для ЛЮБОГО appid, не только «своих» игр владельца, и с ЛЮБЫМ ключом (даже бесплатным личным, не обязательно партнёрским). НЕ путать с GetPlayerAchievements/GetUserStatsForGame по конкретному steamid — та ветка трогает данные ДРУГОГО человека и сознательно НЕ входит в эту задачу, вынесена отдельной развилкой владельцу в DECISIONS.md.

КАК ВСТРОИТЬ: обогатить существующую команду `achievements`, когда ключ задан (имя/описание/hidden рядом с текущими internal id); без ключа — прежнее поведение 1:1, никаких изменений.

ЗАВИСИТ ОТ: SC-20 (конфигурация ключа).

## Ключ Steam Web API: единая конфигурация + контракт деградации

---
id: SC-20
type: tech
status: done
order: 900
notes:
  - id: n1
    text: "Три реальные утечки нашлись чтением кода, а не гаданием: .meta хранил полный URL, текст ошибки его же интерполировал, а curl получал URL в argv (виден в ps). Вырезание ключа сделано ОДНОЙ функцией _redact на всех путях сразу — урок Q-02 про асимметричный guard; и он же тут же подтвердился: guard пустого ответа я поставил в sales, но забыл в wishlist, ревью нашло. Кэш-ключ намеренно РАЗЛИЧАЕТСЯ для разных ключей (соль от sha256 ключа): два партнёра на машине не должны читать портфель друг друга. Флага --api-key нет и не будет."
    createdAt: "2026-07-18T13:10:41.427Z"
links:
  - type: relates
    to: SC-21
  - type: relates
    to: SC-22
---

ЗАЧЕМ: следующий большой ход открывает key-уровень команд (SC-21/SC-22/SC-23/SC-24) — без единой точки конфигурации каждая задача изобретёт свой способ передать ключ, и появится риск размыть key-free identity продукта (README: «no Steam Web API key required» — строка 8, повтор в «Notes & limits» строка 223).

ЧТО СДЕЛАТЬ:
- Один источник ключа: переменная окружения STEAM_CLI_API_KEY (+ алиас STEAM_API_KEY, его экспортируют агенты и CI) с падением на конфиг-файл ~/.config/steam-cli/config.json (0600), который заполняет 'auth set'. НЕ несколько ключей/профилей — владелец подтвердил: маловероятно, что на одной машине несколько партнёрских ключей.
- ФЛАГА --key/--api-key НЕТ И НЕ БУДЕТ (решение раунда 2, не переоткрывать): аргумент командной строки виден другим пользователям машины через ps и оседает в истории шелла — ровно тот вектор утечки, от которого задача защищается, читая ключ в 'auth set' со stdin. Для CI/агентов канал уже закрыт переменной окружения. Прецедент --insecure не переносится: он ослабляет проверку локально, а не выносит секрет наружу. Закреплено тестом test_no_api_key_flag_exists.
- Ключ строго ОПЦИОНАЛЕН и АДДИТИВЕН: ни одна существующая key-free команда не меняет поведение по умолчанию без ключа. Команды, которым нужен ключ, при его отсутствии возвращают {"error","code":"auth"} — не молча ломаются и не деградируют тихо.
- Ключ НИКОГДА не попадает в кэш-файлы, логи, тестовые фикстуры, argv.
- Различай два разных отказа: «ключа нет» (auth, наша сторона) vs «у ключа нет нужного права» (напр. пустой ответ финансов = нет права Sales Data — тоже auth, но со своей подсказкой).

ПРОВЕРЕНО ЖИВОЙ РАЗВЕДКОЙ: api.steampowered.com и partner.steam-api.com отдают идентичные 75 интерфейсов — отдельный «партнёрский домен» городить не нужно, один ключ работает на обоих. ИСКЛЮЧЕНИЕ: IPartnerFinancialsService (SC-24) документирован на partner.steam-api.com и в GetSupportedAPIList не публикуется вовсе.

## Финансовый слой: вишлисты и продажи через IPartnerFinancialsService

---
id: SC-24
type: feature
status: done
order: 1000
notes:
  - id: n1
    text: "Задача существует потому, что я трижды ошибочно заявил владельцу, что вишлистов в API нет, опираясь на GetSupportedAPIList — а IPartnerFinancialsService там не публикуется вовсе (LESSONS/L-0001). Владелец был прав, я нет. Из ловушек: пустой ответ означает отсутствие ПРАВ, а не ноль продаж — переводим в auth-ошибку, иначе владелец примет ноль за факт; sales отдаёт весь аккаунт без фильтра по appid, и строку с нерезолвленным пакетом надо ИСКЛЮЧАТЬ (ревью показало 17-кратное завышение при включении); дни идут последовательно, а не параллельно — Valve ограничивает ключи за частые запросы закрытых дат. sales на реальных данных не проверен: у ключа нет права Sales Data, это действие владельца."
    createdAt: "2026-07-18T13:10:41.525Z"
---

ЗАЧЕМ: владелец с самого начала просил вишлисты и продажи по своим играм; я дважды ошибочно заключил, что таких данных в Web API нет — вывод строился на GetSupportedAPIList, где интерфейс IPartnerFinancialsService НЕ публикуется. Веб-разведка нашла его, живая проверка ключом владельца подтвердила.

ЧТО ДАЁТ (хост partner.steam-api.com, док partner.steamgames.com/doc/webapi/IPartnerFinancialsService):
- GetAppWishlistReporting (выпущен 2026-03-11): wishlist_adds/deletes/purchases/gifts, разбивка по ОС, country_summary[], language_summary[], app_min_date. Параметры key, appid, date. ПРОВЕРЕНО ЖИВЬЁМ на игре владельца — возвращает реальные данные (конкретные цифры намеренно НЕ приводятся здесь: доска в публичном git, это коммерческие данные).
- GetDetailedSales (выпущен 2025-06-26): gross_units_sold, gross_sales_usd, net_sales_usd по packageid/platform/country_code + справочники package_info/app_info/bundle_info/discount_info. Параметры key, date, highwatermark_id. СЕЙЧАС отдаёт пустой response — у ключа нет права Sales Data (нужна Financial API Group в Steamworks). Ждём владельца, код писать можно.
- GetChangedDatesForPartner: даты с изменениями, примитив инкрементальной синхронизации.

ПОДВОДНЫЕ КАМНИ (из доков и ревью, учесть в реализации):
- Часовые пояса РАЗНЫЕ у соседних методов: GetDetailedSales — Pacific, GetAppWishlistReporting — GMT.
- Формат даты плавает: YYYY-MM-DD на входе, YYYY/MM/DD в ответе GetChangedDatesForPartner.
- Числа приходят СТРОКАМИ ('9.9900').
- В GetDetailedSales нет фильтра по appid — отдаётся весь аккаунт, фильтровать на клиенте. ВАЖНО: строку, чей packageid не резолвится в appid (бандл), при фильтре по игре надо ИСКЛЮЧАТЬ, а не включать — иначе чужая выручка попадёт в отчёт по одной игре.
- Пустой ответ = нет прав, а НЕ ноль продаж. Одинаково верно и для вишлистов.
- Valve предупреждает: повторные запросы по закрытым датам могут привести к ограничениям на ключ — кэш закрытых дат длинный, свежих короткий, дни запрашивать последовательно.
- ТЕСТОВЫЕ ФИКСТУРЫ ТОЛЬКО С ВЫДУМАННЫМИ ЧИСЛАМИ: репозиторий публичный, реальные вишлисты/выручка владельца туда попадать не должны.

СНИМАЕТ развилку про cookie-скрейп partner.steamgames.com: официальный API лучше по всем параметрам, скрейп не нужен.

## Портфель владельца ключа: свои игры + история билдов/веток

---
id: SC-22
type: feature
status: done
order: 1100
notes:
  - id: n1
    text: "В 1.5.0 вошла только часть: mygames (портфель по типам). Появилась не по плану, а как побочный эффект бага — сообщение об ошибке в wishlist советовало команду mygames, которой не существовало; теперь есть тест, проверяющий, что команды из подсказок реально есть в парсере. builds/ветки/депоты НЕ сделаны, остаются на следующий ход. Побочно всплыло требование безопасности: mygames показывает и НЕАНОНСИРОВАННЫЕ проекты владельца, поэтому в доках и тестах примеры только выдуманные."
    createdAt: "2026-07-18T13:10:41.624Z"
links:
  - type: relates
    to: SC-20
  - type: relates
    to: SC-23
---

ЗАЧЕМ: владелец просил, чтобы утилита «прекрасно работала с несколькими играми» — без списка своих приложений агент не знает, про какие игры вообще идёт речь, и не может сопоставить имя игры с appid внутри портфеля.

ЧТО ДАЁТ КЛЮЧ (проверено живой разведкой на аккаунте владельца):
- ISteamApps/GetPartnerAppListForWebAPIKey — все приложения аккаунта с типом (game / demo / dlc / music / beta-плейтест). Возвращает В ТОМ ЧИСЛЕ невыпущенные и неанонсированные позиции — поэтому конкретный состав портфеля НЕ выписывается ни сюда, ни в документацию, ни в тесты: доска и репозиторий публичные. В примерах доков — только плейсхолдеры.
- GetAppBuilds / GetAppBetas / GetAppDepotVersions — история билдов с описаниями, ветки, депоты.

СДЕЛАНО В 1.5.0: команда mygames (портфель, группировка по типу, фильтр --type).
НЕ СДЕЛАНО: builds / ветки / депоты — переносится на следующий ход.

## README/README.ru/SKILL.md: секция «для владельцев игр» + реальные пределы партнёрского API

---
id: SC-23
type: docs
status: done
order: 1200
notes:
  - id: n1
    text: "Главное в разделе — не перечисление команд, а ловушка прав: галочка Financial на ключе НЕ равна Financial API Group с правом Sales Data, и на этом владелец потерял время лично. Второе по важности — что пустой ответ Steam нельзя читать как ноль продаж. Исходная формулировка этой задачи содержала прямо ложное утверждение («вишлисты не существуют ни при каком ключе»), которое ушло бы в публичный релиз; исправлено. EN и RU синхронизированы, RU намеренно подробнее — аудитория README.ru нетехническая."
    createdAt: "2026-07-18T13:10:41.726Z"
links:
  - type: relates
    to: SC-21
  - type: relates
    to: SC-22
---

ЗАЧЕМ: identity-риск хода, названный владельцем — продукт был «работает у всех из коробки», рискует стать «часть команд работает только у владельца своих игр» (без ключа они отвечают ошибкой). Документация должна ЧЁТКО развести два лица продукта: hero-обещание «no key required» ОСТАЁТСЯ дефолтом (для обычного пользователя ничего не меняется), ключ — строго опциональная надстройка «для владельцев игр» в отдельном разделе, НЕ в hero/quickstart.

ЧТО ЗАДОКУМЕНТИРОВАНО (сделано в 1.5.0):
1. Как задать ключ: STEAM_CLI_API_KEY / STEAM_API_KEY или 'auth --set' (читает stdin, файл 0600). Флага --api-key намеренно НЕТ — argv виден через ps и оседает в истории шелла. Где взять: partner.steamgames.com → Users & Permissions → Manage Web API Keys.
2. Что открывает ключ — строго по факту реализованного: mygames (портфель), wishlist (вишлисты по своей игре), sales (продажи/выручка). Ачивки со скрытыми именами (SC-21) НЕ вошли в 1.5.0.
3. ЛОВУШКА ПРАВ (главное, на чём теряют время): для 'sales' нужна отдельная Financial API Group с правом Sales Data (Users & Permissions → Manage Groups) — это НЕ галочка Financial на самом ключе, та покрывает только ISteamMicroTxn (внутриигровые покупки). Без права Steam отдаёт ПУСТОЙ ответ, который нельзя принимать за «ноль продаж» — CLI переводит его в ошибку auth с объяснением.
4. ИСПРАВЛЕНО ОТНОСИТЕЛЬНО ПЕРВОНАЧАЛЬНОЙ ФОРМУЛИРОВКИ ЭТОЙ ЗАДАЧИ: ранее здесь стоял «подтверждённый тупик: вишлисты не существуют ни при каком ключе». Это НЕВЕРНО и было следствием опоры на GetSupportedAPIList, где IPartnerFinancialsService не публикуется вовсе (см. LESSONS/L-0001). Вишлисты по СВОИМ играм существуют и работают — GetAppWishlistReporting, проверено живьём. Настоящие пределы, которые и надо документировать: счётчик вишлистов ЧУЖОЙ игры не существует ни при каких правах; трафик страницы и показы — только веб-интерфейс Steamworks, API нет.
5. Разные часовые пояса у соседних методов: wishlist — GMT, sales — US Pacific.
6. Синхронизированы README.md, README.ru.md, skill/SKILL.md, AGENTS.md (конвенция «Bilingual agent-first docs», EN↔RU не разъезжаются). В AGENTS.md «Don't» переписан: прежде там стояло обратное («вишлисты недостижимы, не выдумывай»).

ОТКРЫТАЯ РАЗВИЛКА (владельцу, не решена): читать ли данные ДРУГИХ игроков (чужая библиотека/прогресс по произвольному steamid) — это разворот заявленной в README позиции intentionally out of scope. Живёт в DECISIONS.md, в 1.5.0 не входит.
