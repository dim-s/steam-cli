# LESSONS index — steam-cli

Provenance: заведён 2026-07-14 из аудита `steam_cli.py` (2 сигнала-провала рассмотрены, 0 уроков — оба в карантине после гейта §1.2, ждут независимого повтора).

## active

(пусто)

## candidate

(пусто)

## Отклонено / в карантине

- Q-01 (план→реализация без ре-нумерации): раунд 3 аудита `steam_cli.py` — при реализации одобренного нумерованного плана раунда 2 вставлен незапланированный пункт (`-n 0`), список не ре-нумерован → исходный F4 (curl fallback-regex) потерян, а итоговый отчёт «Сделано F1-F12» сверен по съехавшим лейблам, а не по содержанию плана (источник: менеджер, финальный гейт раунд 3, дословно: «F4 из одобренного плана... не реализован... настоящий F4 (curl) выпал из работы, а отчёт лейбл не сверил»). hits=1, санкции владельца нет → ждём повтор того же механизма (вставка пункта без ре-нумерации + сверка готовности по лейблу, а не по плану) в другой сессии → тогда candidate.
- Q-02 (асимметричный guard в одном диффе): тот же аудит — `cmd_price` создаёт `ThreadPoolExecutor(max_workers=len(regions))` без верхнего предела, хотя тем же диффом на ~200 строк ниже введён `_BROWSE_COUNT_CAP` — guard от fan-out для другого пути; симметричный guard для regions не поставлен (источник: dmitry-programmer, ревью диффа). hits=1 → ждём повтор того же механизма (guard введён для одного fan-out пути в диффе, симметричный путь того же класса оставлен голым) в другой ситуации → тогда candidate.
